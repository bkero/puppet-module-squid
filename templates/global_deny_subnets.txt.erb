<%# Moved to static form as this is global -%>
<%# Also to make the node manifest easier to read -%>
127.0.0.0/8<%# The proxies should never be used to access internal resources %>
::1/128<%# The proxies should never be used to access internal resources %>
10.0.0.0/8<%# The proxies should never be used to access internal resources %>
192.168.0.0/16<%# The proxies should never be used to access internal resources %>
172.16.0.0/12<%# The proxies should never be used to access internal resources %>
